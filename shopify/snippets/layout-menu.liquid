<ul class="{{ class | default: 'flex gap-8 lg:px-5 ' }}" style="{{ style | default: '' }}">
  {% for link in linklists.main-menu.links %}
    <li>
      <a href="{{ link.url }}" class="font-semibold">
        {{ link.title | escape }}
      </a>

      {% if link.levels >= 1 %}
        <div>
          {% for sub_link in link.links %}
            <a href="{{ sub_link.url }}">
              {{ sub_link.title | escape }}
            </a>
          {% endfor %}
        </div>
      {% endif %}
    </li>
  {% endfor %}
</ul>
